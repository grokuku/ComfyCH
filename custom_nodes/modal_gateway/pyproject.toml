[project]
name = "modal-comfyui"
version = "0.1.0"
description = "Run ComfyUI on Modal with auto-scaling, GPU snapshots, and easy model management."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "modal>=1.3.0.post1",
    "httpx>=0.27.0",
    "fastapi>=0.115.0",
]
